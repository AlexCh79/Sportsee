import {useState} from 'react'
import { useNavigate } from 'react-router-dom'
import dataMock from '../../datas/data-mock.js'

const Auth = (username, password) => {
    const navigate = useNavigate()

    
}